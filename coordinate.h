#ifndef COORDINATE_H
#define COORDINATE_H

#include <cmath>	//for sqrt and pow
struct coordinate
{
	double ord[3];
	/*double x=0;
	double y=0;
	double z=0;*/
};
double Distance(coordinate, coordinate);
#endif